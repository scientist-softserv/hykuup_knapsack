# Raised to 4 for the 2026-07-04 bot swarm and still what production runs.
# Drop to 2 once someone confirms the bot traffic is gone.
replicaCount: 4

imagePullSecrets:
  - name: github

resources:
  limits:
    memory: "6Gi"
    cpu: "1500m"
  requests:
    memory: "5Gi"
    cpu: "1250m"

# Chart default path "/" 404s via Hyku's tenant-CNAME check against the kubelet's pod-IP Host header.
# /monitoring/puma_stats (knapsack's own middleware, ahead of Rack::Attack/session/tenant) never touches Solr/DB/cache, so it stays cheap and up even when a dependency or the thread pool is under pressure - same endpoint Site24x7 already polls.
startupProbe:
  enabled: true
  path: "/monitoring/puma_stats"
  failureThreshold: 30
livenessProbe:
  enabled: true
  path: "/monitoring/puma_stats"
  initialDelaySeconds: 15
  periodSeconds: 60
  timeoutSeconds: 60
  failureThreshold: 6
readinessProbe:
  enabled: true
  path: "/healthz"
  initialDelaySeconds: 15
  periodSeconds: 60
  timeoutSeconds: 60
  failureThreshold: 6

brandingVolume:
  storageClass: efs-sc
derivativesVolume:
  storageClass: efs-sc
uploadsVolume:
  storageClass: efs-sc

extraVolumeMounts: &volMounts
  - name: uploads
    mountPath: /app/samvera/hyrax-webapp/tmp/imports
    subPath: imports
  - name: uploads
    mountPath: /app/samvera/hyrax-webapp/tmp/exports
    subPath: exports
  - name: uploads
    mountPath: /app/samvera/hyrax-webapp/public/system
    subPath: public-system
  - name: uploads
    mountPath: /app/samvera/hyrax-webapp/public/uploads
    subPath: public-uploads
  - name: uploads
    mountPath: /app/samvera/hyrax-webapp/tmp/network_files
    subPath: network-files
  - name: uploads
    mountPath: /app/samvera/hyrax-webapp/storage/files
    subPath: storage-files
  - name: uploads
    mountPath: /app/samvera/hyrax-webapp/log/rack_attack
    subPath: rack-attack

ingress:
  enabled: true
  ingressClassName: "nginx-ingress"
  hosts:
    - host: '*.hykuup.com'
      paths:
        - path: /
    - host: mobius-origin.notch8.cloud
      paths:
        - path: /
  annotations: {
    nginx.org/client-max-body-size: "0",
    nginx.org/proxy-connect-timeout: "120s",
    nginx.org/websocket-services: "hykuup-knapsack-production-nginx",
    cert-manager.io/cluster-issuer: letsencrypt-production-dns
  }
  tls:
    - secretName: wildcard-tls-secret
      hosts:
        - '*.hykuup.com'
    - secretName: mobius-origin-tls
      hosts:
        - mobius-origin.notch8.cloud

extraEnvVars: &envVars
  - name: BUNDLE_LOCAL__HYKU_KNAPSACK
    value: /app/samvera
  - name: BUNDLE_DISABLE_LOCAL_BRANCH_CHECK
    value: "true"
  - name: CONFDIR
    value: "/app/samvera/hyrax-webapp/solr/conf"
  - name: DB_ADAPTER
    value: postgresql
  - name: DB_ADVISORY_LOCKS
    value: "false"
  - name: DB_PREPARED_STATEMENTS
    value: "false"
  - name: DB_HOST
    value: acid-postgres-cluster-delta.postgres.svc.cluster.local
  - name: DB_NAME
    value: hykuup_production
  - name: DB_PASSWORD
    value: $DB_PASSWORD
  - name: DB_USER
    value: hykuup_production
  - name: DISABLE_SPRING
    value: '1'
  - name: FCREPO_BASE_PATH
    value: /hykuup
  - name: FCREPO_HOST
    value: fcrepo.fcrepo.svc.cluster.local
  - name: FCREPO_REST_PATH
    value: rest
  - name: FCREPO_PATH
    value: /rest
  - name: FCREPO_URL
    value: http://fcrepo.fcrepo.svc.cluster.local:8080/rest
  - name: HYRAX_ANALYTICS
    value: 'true'
  - name: HYRAX_ANALYTICS_PROVIDER
    value: 'ga4'
  - name: GOOGLE_ANALYTICS_ID
    value: $GOOGLE_ANALYTICS_ID
  # - name: GOOGLE_OAUTH_APP_NAME
  #   value: atla-hyku-production
  # - name: GOOGLE_OAUTH_APP_VERSION
  #   value: '1.0'
  # - name: GOOGLE_OAUTH_PRIVATE_KEY_SECRET
  #   value: $GOOGLE_OAUTH_PRIVATE_KEY_SECRET
  # - name: GOOGLE_OAUTH_PRIVATE_KEY_PATH
  #   value: prod-cred.p12
  # - name: GOOGLE_OAUTH_PRIVATE_KEY_VALUE
  #   value: $GOOGLE_OAUTH_PRIVATE_KEY_VALUE
  # - name: GOOGLE_OAUTH_CLIENT_EMAIL
  #   value: hyku-demo@hyku-demo.iam.gserviceaccount.com
  # Without this DemoTenantResetJob wipes a public demo tenant nightly and
  # restores branding but no works, so the sandbox would empty itself. The
  # %{tenant} placeholder expands to the account name.
  - name: DEMO_SEED_CSV_PATH
    value: "tmp/imports/%{tenant}/seed/metadata.csv"
  - name: HYRAX_ACTIVE_JOB_QUEUE
    value: good_job
  - name: HYRAX_FITS_PATH
    value: /app/fits/fits.sh
  - name: HYRAX_FLEXIBLE
    value: "true"
  - name: HYRAX_VALKYRIE
    value: "true"
  - name: HYKU_ADMIN_HOST
    value: main.hykuup.com
  - name: HYKU_ADMIN_ONLY_TENANT_CREATION
    value: 'false'
  - name: HYKU_ALLOW_SIGNUP
    value: 'false'
  - name: HYKU_ATTACK_RATE_THROTTLE_OFF
    value: "false"
  - name: HYKU_BULKRAX_ENABLED
    value: 'true'
  - name: HYKU_BLOCK_VALKYRIE_REDIRECT
    value: "false"
  # Availability only: use is gated per tenant by the `redirects` Flipflop, and a
  # profile needs a `redirects` hash property before that flag goes on, not after.
  - name: HYRAX_REDIRECTS_ENABLED
    value: "true"
  - name: HYKU_CONTACT_EMAIL
    value: info@hykuup.com
  - name: HYKU_CONTACT_EMAIL_TO
    value: info@hykuup.com
  - name: HYKU_DEFAULT_HOST
    value: "%{tenant}.hykuup.com"
  - name: HYKU_ENABLED
    value: "true"
  - name: HYKU_FILE_ACL
    value: "true"
  - name: HYKU_MULTITENANT
    value: "true"
  - name: HYKU_ROOT_HOST
    value: hykuup.com
  # Shows the controlled vocabularies dashboard link. Upstream withholds it in
  # production by default; HykuUp opts in. Needs `rails populate_qa` after deploy.
  - name: HYKU_VOCABULARY_SIDEBAR_ENABLED
    value: "true"
  - name: INITIAL_ADMIN_EMAIL
    value: $INITIAL_ADMIN_EMAIL
  - name: INITIAL_ADMIN_PASSWORD
    value: $INITIAL_ADMIN_PASSWORD
  - name: HYRAX_USE_SOLR_GRAPH_NESTING
    value: "true"
  - name: IN_DOCKER
    value: "true"
  - name: LD_LIBRARY_PATH
    value: /app/fits/tools/mediainfo/linux
  - name: PASSENGER_APP_ENV
    value: production
  - name: RAILS_CACHE_STORE_URL
    value: redis://:$REDIS_PASSWORD@hykuup-knapsack-production-redis-master:6379/0
  - name: RAILS_ENV
    value: production
  - name: RAILS_LOG_TO_STDOUT
    value: "true"
  - name: RAILS_MAX_THREADS
    value: "5"
  - name: RAILS_SERVE_STATIC_FILES
    value: "true"
  - name: REDIS_HOST
    value: hykuup-knapsack-production-redis-master
  - name: REDIS_PORT
    value: '6379'
  - name: REDIS_PASSWORD
    value: $REDIS_PASSWORD
  - name: REDIS_URL
    value: redis://:$REDIS_PASSWORD@hykuup-knapsack-production-redis-master:6379/0
  - name: SECRET_KEY_BASE
    value: $SECRET_KEY_BASE
  - name: SENTRY_DSN
    value: $SENTRY_DSN
  - name: SENTRY_ENVIRONMENT
    value: "hykuup-knapsack-production"
  - name: SMTP_ADDRESS
    value: smtp.sendgrid.net
  - name: SMTP_DOMAIN
    value: hykuup.com
  - name: SMTP_ENABLED
    value: "true"
  - name: SMTP_PASSWORD
    value: $SMTP_PASSWORD
  - name: SMTP_PORT
    value: "587"
  - name: SMTP_STARTTLS
    value: 'true'
  - name: SMTP_TYPE
    value: plain
  - name: SMTP_USER_NAME
    value: apikey
  - name: SOLR_ADMIN_PASSWORD
    value: $SOLR_ADMIN_PASSWORD
  - name: SOLR_ADMIN_USER
    value: admin
  - name: SOLR_COLLECTION_NAME
    value: hykuup-hyku
  - name: SOLR_COLLECTION_REPLICAS
    value: "3"
  - name: SOLR_CONFIGSET_NAME
    value: hykuup-hyku
  - name: SOLR_HOST
    value: solr-headless.solr
  - name: SOLR_PORT
    value: "8983"
  - name: SOLR_URL
    value: http://admin:$SOLR_ADMIN_PASSWORD@solr-headless.solr:8983/solr/
  - name: SUPPORT_EMAIL
    value: support@notch8.com
  - name: SUPPORT_PASSWORD
    value: testing123
  - name: TEST_USER_EMAIL
    value: user@notch8.com
  - name: TEST_USER_PASSWORD
    value: testing123
  - name: VALKYRIE_ID_TYPE
    value: string
  - name: VALKYRIE_TRANSITION
    value: "true"

podSecurityContext: &podSecValues
  runAsUser: 1001
  runAsGroup: 101
  fsGroup: 101
  fsGroupChangePolicy: 'OnRootMismatch'

worker:
  replicaCount: 1
  resources:
    limits:
      memory: "4Gi"
      cpu: "1000m"
    requests:
      memory: "4Gi"
      cpu: "100m"
  extraVolumeMounts: *volMounts
  extraEnvVars: *envVars
  podSecurityContext: *podSecValues

embargoRelease:
  enabled: false
leaseRelease:
  enabled: false

fcrepo:
  enabled: false
postgresql:
  enabled: false
redis:
  image:
    repository: bitnamilegacy/redis
    tag: 7.0.2-debian-11-r7
  architecture: standalone
  master:
    # maxmemory kept below the container limit so Redis evicts stale cache
    # entries under its own eviction policy instead of relying solely on the
    # container limit + kernel OOM killer.
    # volatile-lru: ~98.5% of current keys already carry a TTL (long-dated
    # Rails.cache entries), so this evicts from that pool under pressure
    # without touching the small fraction of keys with no expiry at all.
    configuration: |-
      maxmemory 2gb
      maxmemory-policy volatile-lru
    # TODO: shrink limit/request to 3072Mi/2560Mi (headroom above maxmemory,
    # rather than an independent ceiling) once a deploy with maxmemory has
    # been running healthily in production for a while. Left matching the
    # current live 4Gi/2Gi for now so this PR only adds the eviction policy
    # on the next deploy, without also surprise-shrinking an already-running
    # container's memory ceiling in the same step.
    resources:
      limits:
        memory: "4096Mi"
        cpu: "100m"
      requests:
        memory: "2048Mi"
        cpu: "20m"
    persistence:
      enabled: true
      storageClass: gp2
  auth:
    password: $REDIS_PASSWORD

solr:
  enabled: false

externalFcrepoHost: fcrepo.fcrepo.svc.cluster.local
externalPostgresql:
  host: acid-postgres-cluster-delta.postgres.svc.cluster.local
  username: hykuup_production
  password: $DB_PASSWORD
  database: hykuup_production

externalSolrPassword: $SOLR_ADMIN_PASSWORD
externalSolrHost: solr.solr.svc.cluster.local
externalSolrUser: admin
externalSolrCollection: "hykuup-hyku"

global:
  hyraxHostName: hykuup-knapsack-production-hyrax

nginx:
  enabled: true
  service:
    type: ClusterIP
  image:
    # Built from the `hyku-nginx` Dockerfile stage, which bakes public/assets,
    # public/pdf.js, and public/uv in from the `hyku-web` stage of the exact
    # same commit - no shared volume/copy step needed for these anymore.
    # Still based on registry.gitlab.com/notch8/scripts/bitnami-nginx (see
    # Dockerfile) since the server block below references that image's
    # bots.d/{ddos,blockbots}.conf includes.
    registry: ghcr.io
    repository: "$REPO_LOWER/nginx"
    tag: "$TAG"
  extraVolumes:
    - name: "uploads"
      persistentVolumeClaim:
        claimName: "{{ .Values.global.hyraxHostName }}-uploads"
    - name: "branding"
      persistentVolumeClaim:
        claimName: "{{ .Values.global.hyraxHostName }}-branding"
  extraVolumeMounts:
    - name: uploads
      mountPath: /app/samvera/hyrax-webapp/public/system
      subPath: public-system
    - name: branding
      mountPath: /app/samvera/hyrax-webapp/public/branding

  # Serves /system (Site branding images: logo/banner/favicon) and /branding
  # (per-tenant branding assets) directly from shared EFS PVCs so these
  # requests never tie up a Puma worker.
  # NOTE: deliberately excludes /uploads/, unlike /system/ that directory may
  # hold access-controlled content Rails is supposed to gate - matches the
  # convention already used for other notch8 Hyrax apps' nginx server blocks.
  # All other paths fall through to the Rails app unchanged.
  serverBlock: |-
    upstream rails_app {
      server {{ .Values.global.hyraxHostName }};
    }

    map ${DOLLAR}status ${DOLLAR}loggable {
        ~^444  0;
        default 1;
    }

    log_format loki 'host=${DOLLAR}host ip=${DOLLAR}http_x_forwarded_for remote_user=${DOLLAR}remote_user [${DOLLAR}time_local] '
                      'request="${DOLLAR}request" status=${DOLLAR}status bytes=${DOLLAR}body_bytes_sent '
                      'referer="${DOLLAR}http_referer" agent="${DOLLAR}http_user_agent" request_time=${DOLLAR}request_time upstream_response_time=${DOLLAR}upstream_response_time upstream_response_length=${DOLLAR}upstream_response_length';

    error_log  /opt/bitnami/nginx/logs/error.log warn;

    # Lets ActionCable's websocket handshake upgrade instead of falling through to a plain HTTP request Rails 404s.
    map ${DOLLAR}http_upgrade ${DOLLAR}connection_upgrade {
        default upgrade;
        ''      '';
    }
    # Cloudflare ips see for refresh
    # https://support.cloudflare.com/hc/en-us/articles/200170786-Restoring-original-visitor-IPs-logging-visitor-IP-addresses
    # update list https://www.cloudflare.com/ips/
    set_real_ip_from 103.21.244.0/22;
    set_real_ip_from 103.22.200.0/22;
    set_real_ip_from 103.31.4.0/22;
    set_real_ip_from 104.16.0.0/13;
    set_real_ip_from 104.24.0.0/14;
    set_real_ip_from 108.162.192.0/18;
    set_real_ip_from 131.0.72.0/22;
    set_real_ip_from 141.101.64.0/18;
    set_real_ip_from 162.158.0.0/15;
    set_real_ip_from 172.64.0.0/13;
    set_real_ip_from 173.245.48.0/20;
    set_real_ip_from 188.114.96.0/20;
    set_real_ip_from 190.93.240.0/20;
    set_real_ip_from 197.234.240.0/22;
    set_real_ip_from 198.41.128.0/17;
    set_real_ip_from 2400:cb00::/32;
    set_real_ip_from 2606:4700::/32;
    set_real_ip_from 2803:f800::/32;
    set_real_ip_from 2405:b500::/32;
    set_real_ip_from 2405:8100::/32;
    set_real_ip_from 2a06:98c0::/29;
    set_real_ip_from 2c0f:f248::/32;

    real_ip_header X-Forwarded-For;
    real_ip_recursive on;
    include /opt/bitnami/nginx/conf/conf.d/*.conf;
    server {
        listen 8080;
        server_name _;
        root /app/samvera/hyrax-webapp/public;
        index index.html;

        client_body_in_file_only clean;
        client_body_buffer_size 32K;
        client_max_body_size 0;
        access_log /opt/bitnami/nginx/logs/access.log loki;

        sendfile on;
        send_timeout 300s;

        include /opt/bitnami/nginx/conf/bots.d/ddos.conf;
        include /opt/bitnami/nginx/conf/bots.d/blockbots.conf;

        location ~ (\.php|\.aspx|\.asp) {
        	return 404;
        }

        # deny requests for files that should never be accessed
        location ~ /\. {
          deny all;
        }

        location ~* ^.+\.(rb|log)${DOLLAR} {
          deny all;
        }
        # serve static (compiled) assets / branding images directly if they exist
        location ~ ^/(assets|branding|packs|fonts|images|javascripts|stylesheets|swfs|system|uv|pdf\.js)/ {
          try_files ${DOLLAR}uri @rails;

          gzip_static on; # to serve pre-gzipped version

          expires max;
          add_header Cache-Control public;

          # Some browsers still send conditional-GET requests if there's a
          # Last-Modified header or an ETag header even if they haven't
          # reached the expiry date sent in the Expires header.
          add_header Last-Modified "";
          add_header ETag "";
          break;
        }

        # send non-static file requests to the app server
        location / {
          try_files ${DOLLAR}uri @rails;
        }

        location @rails {
          proxy_set_header  X-Real-IP  ${DOLLAR}remote_addr;
          proxy_set_header  X-Forwarded-For ${DOLLAR}proxy_add_x_forwarded_for;
          proxy_set_header Host ${DOLLAR}http_host;
          proxy_redirect off;
          proxy_pass http://rails_app;
        }
    }

# The fits subchart hardcodes both probes with no timeout, so Kubernetes' 1s
# default applies and the kubelet SIGKILLs FITS while it is analyzing a large
# file. A 100 MB PDF measured at 282s, so such files could never characterize.
# Runs our own deployment until samvera-labs/fits-charts#6 makes the probes
# configurable, then this reverts to `fits.enabled: true`.
fits:
  enabled: false

externalFits:
  enabled: true
  url: http://hykuup-knapsack-production-fits:8080/fits

extraDeploy:
  - |
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: {{ .Release.Name }}-fits
      labels:
        app.kubernetes.io/name: fits
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      replicas: 1
      strategy:
        type: Recreate
      selector:
        matchLabels:
          app.kubernetes.io/name: fits
          app.kubernetes.io/instance: {{ .Release.Name }}
      template:
        metadata:
          labels:
            app.kubernetes.io/name: fits
            app.kubernetes.io/instance: {{ .Release.Name }}
        spec:
          restartPolicy: Always
          containers:
            - name: fits
              image: ghcr.io/samvera/fitsservlet:1.6.0
              imagePullPolicy: IfNotPresent
              ports:
                - name: http
                  containerPort: 8080
                  protocol: TCP
              # Blocking on a long analysis is normal for FITS, not ill health.
              # Both windows exceed the slowest observed analysis; with one
              # replica a failing readiness probe leaves the Service with no
              # endpoint, so it has to tolerate the same duration as liveness.
              readinessProbe:
                httpGet:
                  path: /fits
                  port: http
                initialDelaySeconds: 15
                periodSeconds: 30
                timeoutSeconds: 10
                failureThreshold: 30
              livenessProbe:
                httpGet:
                  path: /fits
                  port: http
                initialDelaySeconds: 60
                periodSeconds: 30
                timeoutSeconds: 30
                failureThreshold: 40
              resources:
                limits:
                  memory: "4Gi"
                  cpu: "1000m"
                requests:
                  memory: "4Gi"
                  cpu: "250m"
  - |
    apiVersion: v1
    kind: Service
    metadata:
      name: {{ .Release.Name }}-fits
      labels:
        app.kubernetes.io/name: fits
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      type: ClusterIP
      ports:
        - port: 8080
          targetPort: http
          protocol: TCP
          name: http
      selector:
        app.kubernetes.io/name: fits
        app.kubernetes.io/instance: {{ .Release.Name }}
