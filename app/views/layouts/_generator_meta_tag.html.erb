<%# Hyrax's layouts/_head_tag_content.html.erb renders 'layouts/generator_meta_tag'
    but doesn't ship a default for it - it's expected to be provided by the
    app. Missing entirely in this knapsack, which breaks every page
    (ActionView::MissingTemplate), unrelated to any other change. %>
<meta name="generator" content="hykuUP" />
